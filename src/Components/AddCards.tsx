import React from 'react';
import { Link, RouteComponentProps } from '@reach/router';


const AddCards: React.FC<RouteComponentProps> = () => {
  
  return (
    <Link to='home'>Back to home</Link>
  );
}

export default AddCards;