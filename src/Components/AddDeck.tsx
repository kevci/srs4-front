import React from 'react';
import { Link, RouteComponentProps } from '@reach/router';

const AddDeck: React.FC<RouteComponentProps> = () => {
	
	return (
		<>
			<Link to='home'>Back to Home</Link>
		</>
	);
}

export default AddDeck;